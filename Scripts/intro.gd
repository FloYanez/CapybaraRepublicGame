extends Control

var end_intro = false # que al final del dialogo cambi esta variable a true, y al final tiene que haber una funcion que se active es verdadera y pase a la siguiente escena
# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
