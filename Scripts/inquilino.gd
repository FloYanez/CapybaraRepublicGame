extends Resource
class_name Inquilino

export (Texture) var sprite

export (int) var corazones_inicial = 6

export (int) var corazones_cortar_luz = 0
export (int) var corazones_cortar_agua = 0
export (int) var corazones_ratas = 0

# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
